<div class="dashboard-container">
    <aside class="sidebar">
        <div class="sidebar-header">
            <span class="sidebar-logo-section">
                <img src="" class="sidebar-logo">
                <span class="brand-name">DeepFocus Workflow</span>
            </span>
            <span class="sidebar-toggle">
                <i class="fa-solid fa-bars"></i>
            </span>
        </div>


        <nav class="nav-menu">
            <div class="nav-group">
                <a href="/projects" class="nav-item active">
                    <i class="fa-solid fa-cube"></i>
                    Projects
                </a>
            </div>

            <div class="nav-group bottom-nav">
                <a href="/calendar" class="nav-item">
                    <i class="fa-regular fa-calendar"></i>
                    Calendar
                </a>
                <a href="/analytics" class="nav-item">
                    <i class="fa-solid fa-chart-simple"></i>
                    Analytics
                </a>
                <a href="/settings" class="nav-item">
                    <i class="fa-solid fa-gear"></i>
                    Settings
                </a>
            </div>
        </nav>
    </aside>

    <main class="main-content">
        <header class="content-header">
            <div class="header-titles">
                <h1>Projects</h1>
                <p>Choose what you want to focus on today.</p>
            </div>
            <div class="header-actions">
                <img src="{{user.displayImage}}" class="avatar" referrerPolicy="no-referrer">
            </div>
        </header>

        <div class="action-bar">
            <button class="new-project-btn">
                <i class="fa-solid fa-plus"></i>
                New Project
            </button>
        </div>

        <section class="project-grid">
            {{#if projects}}
                {{#each projects}}
                    <div class="project-card">
                        <div class="card-header">
                            <div class="project-icon-box purple-bg">
                                <i class="fa-solid fa-cube"></i>
                            </div>
                            <div class="project-info">
                                <h3>{{this.projectName}}</h3>
                                <p>{{this.projectDesc}}</p>
                            </div>
                        </div>
                        <button class="btn-outline"><i class="fa-solid fa-play"></i> Open Project</button>
                    </div>
                {{/each}}
            {{else}}
                No projects...
            {{/if}}

            <!-- Additional cards follow same structure -->
        </section>
    </main>
</div>
